<%#<h2>Edit <%= resource_name.to_s.humanize % ></h2>%>
<h2>Compte Utilisateur</h2>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
  <%= devise_error_messages! %>

  <div><%= f.label :pseudo %><br />
  <%= f.text_field :pseudo, :autofocus => true %></div>

  <div><%= f.label :email %><br />
  <%= f.email_field :email %></div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div><%= f.label :password, 'Nouveau mot de passe' %> <i>(laissez vide si vous ne voulez pas le modifier)</i><br /> <%#<i>(leave blank if you don't want to change it)</i><br />%>
  <%= f.password_field :password, :autocomplete => "off" %></div>

  <div><%= f.label :password_confirmation, 'Confirmation du nouveau mot de passe' %><br />
  <%= f.password_field :password_confirmation %></div>

  <div><%= f.label :current_password, 'Mot de passe' %> <i>(votre mot de passe actuel est nécessaire pour enregistrer les modifications)</i><br /> <%#<i>(we need your current password to confirm your changes)</i><br />%>
  <%= f.password_field :current_password %></div>

  <%#<div><%= f.submit "Update" % ></div>%>
  <div><%= f.submit "Enregistrer" %></div>
<% end %>

<h3>Supprimer mon compte</h3>

<p>Déçu(e) par le service ? <%= button_to "Supprimer mon compte", registration_path(resource_name), :data => { :confirm => "Êtes-vous sûr(e)?" }, :method => :delete %></p>

<%#= link_to "Back", :back %>
